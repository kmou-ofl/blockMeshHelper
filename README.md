# blockMeshHelper

# usage

```bash
cd $HOME
git clone git@github.com:kmou-ofl/blockMeshHelper.git
cd blockMeshHelper
g++ blockMeshHelper.cpp -o blockMeshHelper

# to generate 3*2*1 blocks for x*y*z, type

./blockMeshHelper 4 3 2

# and copy the output and replace blockMeshDict with it.
```
